/**
 * @desc action info
 * @type {EditorPluginInfo}
 */
const info = {
  name: '页面通用上报',
  desc: '上报页面和组件通用数据',
  author: '',
};

/**
 * @desc component styleSchema
 * @type {EditorPlugin}
 */
export default { info, __isBuildIn: true };
